# Required: strong random string for Payload sessions
PAYLOAD_SECRET=your-secret-key-here

# Required: PostgreSQL connection (local or Neon/Vercel Postgres)
# Local Docker: postgresql://postgres:postgres@localhost:5432/catarina
POSTGRES_URL=postgresql://postgres:postgres@localhost:5432/catarina

# Optional: defaults work for local dev
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3000
NEXT_PUBLIC_PAYLOAD_URL=http://localhost:3000

# Optional: Resend API key for email notifications (get from resend.com/api-keys)
# If not set, emails will be logged to console in development
RESEND_API_KEY=re_xxxxxxxxx

# Optional: From address for emails (use your verified domain in production)
# Default: onboarding@resend.dev (works without domain verification)
# Production: alerts@catarina.app (requires domain verification in Resend)
RESEND_FROM_ADDRESS=onboarding@resend.dev
